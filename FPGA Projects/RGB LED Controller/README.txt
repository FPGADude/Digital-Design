For the RGB LED controller using the BASYS 3 FPGA board.

The RGB used in the design is a common cathode and set into a breadboard.

The 3 anodes are driven by the BASYS 3 Pmod.

A 330 ohm resistor is used on the cathode of the RGB.

David J Marion